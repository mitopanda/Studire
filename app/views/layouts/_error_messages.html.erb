<% if model.errors.any? %>
      <div class="alert alert-warning">
        <ul>
          <% model.errors.full_messages.each do |message| %>
            <li><%= message, class: 'text-center' %></li>
          <% end %>
        </ul>
      </div>
    <% end %>
