<div class="col-xs-10 offset-xs-1 col-md-6 offset-md-3 col-lg-4 offset-lg-4"> 
  <div class="text-center new-user-title">Studireにログイン</div>

  <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
    <div class="field form-group">
      <%= f.email_field :email, autofocus: true, autocomplete: "email", id: 'email', class: 'form-control', placeholder: 'メールアドレス' %>
    </div>

    <div class="field form-group">
      <%= f.password_field :password, autocomplete: "current-password", id: 'password', class: 'form-control', placeholder: 'パスワード' %>
    </div>
      
    <div class="field">
      <% if devise_mapping.rememberable? %>
        <%= f.check_box :remember_me %>
        <%= f.label :remember_me, '次回から自動的にログイン' %>
      <% end %>
    </div>

    <div class="actions">
      <%= f.submit "ログイン", class: "btn btn-outline-primary w-100 mb-2" %>
    </div>
  <% end %>

  <div>
    <p>アカウントをお持ちでない方は
    <%- if devise_mapping.registerable? && controller_name != 'registrations' %>
      <%= link_to "新規登録", new_registration_path(resource_name) %>
    <% end %>
    からお願いいたします。
    </p>
    <!--<p>パスワードをお忘れの方は
    <%#- if devise_mapping.recoverable? && controller_name != 'passwords' && controller_name != 'registrations' %>
      <%#= link_to "こちらへ。", new_password_path(resource_name) %><br />
    <%# end %>
    </p> %>-->
  </div>
  <hr>
  <%#= render "users/shared/links" %>
  <div class="sns-login">
    <%= link_to user_twitter_omniauth_authorize_path, class: 'btn btn-lg btn-twitter w-100' do %>
      <i class="fab fa-twitter fa-1x"></i> Twitterアカウントでログイン
    <% end %>
  </div>
</div>